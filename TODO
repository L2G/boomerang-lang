
== BUILD == 

* add target for bytecode compilation (Nate)

== SYSTEM ==

* -check-only flag: don't compile and run, just check that base sorts
  line up.

== LIBRARIES == 

* Detangle sorting from XML and put it in own module (Nate).

* Fix XML to use Escaping.

* Expose permute in Core (probably adding a primitive helper to write
  its contract)

* Fix contract on q-lenses, reflecting "rel" out of Blenses.t.

* Add refinements to addressing.boom, bibtex.boom.

== DOCUMENTATION ==

* Polish intro of manual.

* Add examples using q-lenses to QuickStart.

* Add description of sync.
