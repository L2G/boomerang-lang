OCAMLPACKS += num
OCAML_LIBS += $(SRCDIR)/harmony 

RESULT = boomerang
RESULTS = $(RESULT)

BOOMERANG_SOURCES[] = rint.ml rstring.ml \
                      crm.ml erx.ml \
                      rlenses.ml rvalue.ml rparser.mly rlexer.mll \
                      main.ml

OCamlProgram($(RESULT), $(rootname $(BOOMERANG_SOURCES)))

.DEFAULT: $(RESULT)

CLEAN += rparser.ml rparser.mli rlexer.ml

clean:
	$(CLEAN) $(RESULT) 
