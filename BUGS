module Fail =
module Bar =
  type t = Unit of unit (* or anything else *)
  let f (x:t) = true
  let g (x:t where f x) = ()
end

test Bar.g (Bar.Unit ()) = ? (* BUG: f unbound *)

(* Not really a bug: *)
test 2-1 = error (* "-1 is not bound" *)
test 2 - 1 = 1 (* ok *)
