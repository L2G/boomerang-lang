OCAMLINCLUDES += \
  $(UBASEDIR) \
  $(HBASEDIR) \
  $(BOOMERANGDIR)/src

OCAML_LIBS += \
  $(UBASEDIR)/ubase \
  $(HBASEDIR)/hbase \
  $(BOOMERANGDIR)/src/boomerang

OCAML_OTHER_LIBS += str

RESULT = learn
RESULTS = $(RESULT)

LEARNING_SOURCES[] = regexp.ml lex.ml hist.ml oracle.ml struct.ml lenses.ml driver.ml

OCamlProgram($(RESULT), $(rootname $(LEARNING_SOURCES)))

.DEFAULT: $(RESULTS)