//   SEND      9567
//+  MORE   +  1085
//-------   -------
//= MONEY   = 10652
//
// Y = 2, R = 8, O = 0, D = 7, N = 6, E = 5, M = 1, S = 9
//
E. r1 (E. r2 (E. r3 (E. r4 (

//
// constraints on carries
//
r1 in [0,1] && r2 in [0,1] && r3 in [0,1] && r4 in [0,1] &&

//
// constraints on letters
//
s in [1,9] && m in [1,9] && // the first letter can't be 0
e in [0,9] && n in [0,9] && d in [0,9] && o in [0,9] && r in [0,9] && y in [0,9] &&


//
// description of the addition
//
   d+e = 10*r1+y &&
r1+n+r = 10*r2+e &&
r2+e+o = 10*r3+n &&
r3+s+m = 10*r4+o &&
r4     = m

&&

//
// all letters have different values
//
s != e && s != n && s != d && s != m && s != o && s != r && s != y &&
e != n && e != d && e != m && e != o && e != r && e != y &&
n != d && n != m && n != o && n != r && n != y &&
d != m && d != o && d != r && d != y &&
m != o && m != r && m != y &&
o != r && o != y &&
r != y

))))
