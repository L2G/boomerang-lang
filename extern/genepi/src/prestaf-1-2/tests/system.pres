/*
 node Switch
  flow  i : int
        o1 : int
        o2 : int
  state up : bool
  event push; // not coded 
  assert up => (o1 = i) && not up => (o2=i)
  trans     up |- push -> up := false
        not up |- push -> up := true
  init up = true
 */



/*
 * init 
 */
   up >= 0
&& o1 >= 0
&& o2 >= 0
&& i >= 0
&&(
  up == 1
);

/*
 * assert 
 */
// the assertion


    (up == 1 => o1==i)
&&  (! (up == 1) => o2==i)

// domain constraint for booleans
&&  (up == 0 || up == 1)
&&  (i == 0 || i == 1 )
&&  (o2 == 0 || o2 == 1 )
&&  (o1 == 0 || o1 == 1 )
;
/*
 * trans
 */
   up_p >= 0
&& o1_p >= 0
&& o2_p >= 0
&&  i_p >= 0
&&(
   ((up == 1) && (up_p == 0))
|| ((! (up == 1)) && (up_p == 1))
)
